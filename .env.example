# Public environment variables (exposed to client)
PUBLIC_CTA_URL=https://calendly.com/your-link

# Private environment variables (server-side only)
# Add secret keys here - they won't be exposed to the client
